@model List<PatisserieCD.Models.Category>

<h1>Categories</h1>

@foreach (var category in Model)
{
    <p>
        <a asp-controller="Categories"
           asp-action="Details"
           asp-route-id="@category.Id">
            @category.Name
        </a>
    </p>
}